# Cache de descargas de OpenStreetMap (se regenera con el pipeline)
cache/

# Python
__pycache__/
*.pyc
.venv/
venv/

# Los datos generados en docs/data/ SÍ se versionan a propósito: son unos pocos
# MB y son lo que hace que el sitio publicado funcione sin correr el pipeline.

# Sistema
Thumbs.db
desktop.ini
.DS_Store
